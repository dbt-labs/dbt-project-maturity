version: 2

models: 
  - name: fct_billed_patient_claims
    description: record per claim with payment, patient and diagnosis data
    columns:
      - name: claim_id
        tests:
          - unique
          - not_null